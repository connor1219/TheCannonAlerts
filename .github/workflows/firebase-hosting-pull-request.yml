# DISABLED: CI/CD workflow is not needed for this project and incurs costs.
# Keeping this file to demonstrate knowledge of GitHub Actions workflows.
# To enable, uncomment the workflow below.

# name: Deploy to Firebase Hosting on PR
# on:
#   pull_request:
#     branches:
#       - prod
# permissions:
#   checks: write
#   contents: read
#   pull-requests: write
# jobs:
#   build_and_preview:
#     if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - run: yarn install --frozen-lockfile && yarn build
#         working-directory: frontend
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: ${{ secrets.GITHUB_TOKEN }}
#           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_THECANNONMONITOR }}
#           projectId: thecannonmonitor
#         env:
#           FIREBASE_CLI_EXPERIMENTS: webframeworks
