rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client access - only allow server-side Firebase Functions
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
